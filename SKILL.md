---
name: talbs-whatsapp-bot
description: התקנה, עדכון ותפעול של "העוזר האישי" — עוזר WhatsApp שרץ על המחשב של המשתמש ומחובר ל-Claude Code שלו (אפס עלות API נוספת). להפעיל כשהמשתמש אומר "להתקין את העוזר האישי", "לחבר את הוואטסאפ ל-Claude", "העוזר לא עונה", "/talbs-whatsapp-bot", או מבקש לעדכן או לתקן את העוזר.
user-invocable: true
---

# העוזר האישי · טל בשור — התקנה ותפעול

מטרה: להביא את המשתמש מאפס לעוזר WhatsApp פעיל תוך כ-5 דקות, **בפקודה אחת**. לא בונים ידנית, לא מעתיקים קבצים — המתקין עושה הכל ומאמת בסוף שמה שרץ הוא באמת הגרסה החדשה.

## מה זה

- עוזר אישי בוואטסאפ שרץ **על המחשב של המשתמש** ומדבר עם **Claude Code שכבר מותקן אצלו** — בלי מפתח API נפרד, בלי עלות נוספת.
- **מדבר רק עם המספר שחיבר אותו** (הצ'אט של המשתמש עם עצמו). כל מספר אחר נחסם אוטומטית.
- הזהות שלו = קובץ הנחיות (`העוזר-שלי.md`) בתיקיית העבודה, שנערך מהמסך או ישירות במחשב.
- שתי דרכי חיבור: **Green API** (מומלץ, יציב) או **כרום/QR** (חינמי לגמרי, מתנתק לפעמים).
- אופציונלי: **הודעות קוליות** (תמלול דרך OpenAI, מפתח ב-`.env`) ו**האזנה לקבוצות** (קריאה בלבד — לעולם לא כותב בקבוצה).

## שפה

בטקסט למשתמש קוראים לזה **"העוזר האישי"** — לא "בוט". פונים בלשון ניטרלית (לוחצים / מדביקים / בוחרים), לא בלשון זכר או נקבה.

## התקנה (מק) — פקודה אחת

```bash
curl -sL https://raw.githubusercontent.com/talbs1988-dotcom/talbs-whatsapp-bot/main/template/install.sh | bash
```

מה הפקודה עושה, בסדר הזה:

1. **בדיקות מקדימות** — Node.js 18+, npm, Claude Code. חסר משהו → הודעה בעברית מה להתקין, ושום דבר לא משתנה.
2. **עוצרת גרסה ישנה** אם רצה (השירות + מי שמאזין על הפורט 7655).
3. מורידה את הקוד ל-`~/talbs-whatsapp-bot/`, **ושומרת** הגדרות, חיבור, סודות ולוגים של התקנה קודמת.
4. מתקינה תלויות ורושמת שירות (`launchd`) — העוזר עולה לבד גם אחרי הפעלה מחדש של המחשב.
5. **מאמתת** שהדף שהשרת מגיש נושא את מזהה הגרסה שירדה עכשיו. רק אז ✅ ופתיחת הדפדפן. אם לא — ⚠️ עם שורות הלוג האחרונות.

**אל תשאלו על מספר טלפון.** המספר נכנס אוטומטית מהסריקה/מהחיבור.
**אל תבקשו טוקנים בצ'אט.** מפתחות (Green API, OpenAI) מדביקים במסך העוזר → נשמרים ב-`.env` בלבד.

## אחרי ההתקנה — מה להגיד למשתמש

> **מוכן. הדפדפן נפתח ב-http://127.0.0.1:7655 עם המסך של העוזר.**
>
> 1. **מחברים את הוואטסאפ** — במסך הראשי מופיע QR. בטלפון: WhatsApp → ⚙️ הגדרות → מכשירים מקושרים → קישור מכשיר → סורקים.
> 2. **שולחים לעצמכם הודעה** — פותחים את הצ'אט שלכם עם עצמכם בוואטסאפ ושולחים "היי". העוזר עונה תוך כמה שניות.
> 3. **מגדירים אותו** — בטאב ⚙️ הגדרות: קובץ ההנחיות (מי הוא ומה הוא יודע), התיקייה שהוא עובד בה, ואם רוצים — Green API, הודעות קוליות, קבוצות.

## עדכון לגרסה חדשה (בלי לאבד חיבור והגדרות)

```bash
curl -sL https://raw.githubusercontent.com/talbs1988-dotcom/talbs-whatsapp-bot/main/template/upgrade.sh | bash
```

## איפה מה

| מה                                  | איפה                                                        |
| ----------------------------------- | ----------------------------------------------------------- |
| הקוד והמסך                          | `~/talbs-whatsapp-bot/` (`bot.js`, `index.html`)            |
| הגדרות (שם, תיקייה, מודל, קבוצות)   | `~/talbs-whatsapp-bot/config.json`                          |
| סודות (טוקן Green API, מפתח OpenAI) | `~/talbs-whatsapp-bot/.env` — הרשאות 600, לא ב-git, לא במסך |
| חיבור וואטסאפ (כרום)                | `~/talbs-whatsapp-bot/auth/`                                |
| קובץ ההנחיות של העוזר               | `<תיקיית העבודה>/העוזר-שלי.md`                              |
| הודעות מקבוצות שנבחרו               | `<תיקיית העבודה>/קבוצות/<שם הקבוצה>.md`                     |
| לוג                                 | `~/talbs-whatsapp-bot/logs/assistant.log`                   |
| השירות                              | `~/Library/LaunchAgents/com.talbs.workshop-bot.plist`       |
| המסך                                | http://127.0.0.1:7655                                       |

## פתרון תקלות

| תופעה                                 | מה עושים                                                                                                                                 |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| "Claude Code לא נמצא" / העוזר לא עונה | מתקינים ומתחברים ל-Claude Code, ואז במסך: הגדרות ← "בדוק ש-Claude עונה" (בודק גם שהוא מחובר לחשבון, לא רק מותקן)                         |
| המסך לא נפתח                          | http://127.0.0.1:7655 ידנית. לא עונה? `tail -n 30 ~/talbs-whatsapp-bot/logs/assistant.log`                                               |
| העוזר מגיב לאט או לא מגיב (Green API) | לעדכן לגרסה העדכנית (upgrade). המסך הראשי מציג מה המצב וכמה זמן עבר מהמשיכה האחרונה                                                      |
| "ב-Green API מוגדרת כתובת webhook"    | בקונסול של Green: הגדרות ← Webhook URL ← למחוק, ואז במסך "ניקיתי — נסה להתחבר שוב"                                                       |
| הודעה קולית לא מתומללת                | הגדרות ← 🎤 הודעות קוליות ← "בדיקת מפתח". 401 = מפתח שגוי, 429 = אין קרדיט ב-OpenAI                                                      |
| הקבוצה לא נשמרת לקובץ                 | הקבוצה צריכה להיות מסומנת ו"שמירת הגדרות" נלחץ; העוזר שומר רק הודעות טקסט                                                                |
| להחליף טלפון/מספר                     | במסך: "התחבר מחדש" → סריקה מהטלפון החדש. רק המספר החדש ידבר עם העוזר                                                                     |
| להסיר לגמרי                           | `launchctl bootout gui/$(id -u)/com.talbs.workshop-bot; rm -rf ~/talbs-whatsapp-bot ~/Library/LaunchAgents/com.talbs.workshop-bot.plist` |

## כללי ברזל לסוכן שמפעיל את הסקיל

- **לא לבנות ידנית** ולא להעתיק קבצים "כמו המתקין" — רק הפקודה. המתקין הוא מקור האמת.
- **"הפעולה הצליחה" ≠ "הדבר עובד".** אחרי התקנה/עדכון — לבדוק ש-`curl -s http://127.0.0.1:7655/state` מחזיר JSON ושהמסך נפתח. המתקין כבר עושה את זה; לא לדווח ✅ אם הוא דיווח ⚠️.
- **סודות רק ב-`.env`.** אף פעם לא לבקש טוקן בצ'אט, לא לכתוב אותו ב-`config.json`, לא להדפיס אותו.
- **לא לגעת בהתקנות אחרות** של וואטסאפ במחשב (תיקיות/פורטים אחרים) — רק `~/talbs-whatsapp-bot` ופורט 7655.
